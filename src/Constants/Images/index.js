const base = "/images";
const projects = base + "/Projects";

export const GLOBAL = base + "/Global";
export const HOME = base + "/Home";
export const TEMPLATES = projects + "/Templates";
export const APPS = projects + "/Apps";
